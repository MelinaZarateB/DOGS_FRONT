import styles from '../SideBar/SideBar.module.scss';

const SideBar = () => {
    return(
        <div className={styles.SideBar}>
            Barra Lateral
        </div>
    )

}

export default SideBar;